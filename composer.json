{
    "name": "ntphuc/simple-memo-app",
    "description": "Simple PHP memo app with SQLite, reminders, and an optional Electron wrapper.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": "^8.5"
    },
    "require-dev": {
        "laravel/pint": "^1.29",
        "phpstan/phpstan": "^2.2",
        "phpunit/phpunit": "^13.0",
        "rector/rector": "^2.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse",
        "pint": "pint",
        "pint:test": "pint --test",
        "rector": "rector process",
        "rector:dry": "rector process --dry-run",
        "check": [
            "@pint:test",
            "@analyse",
            "@test",
            "composer audit"
        ]
    },
    "config": {
        "sort-packages": true
    }
}
